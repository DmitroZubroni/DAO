import { Form,  FormGroup, FormLabel} from "react-bootstrap";

const GetProposes = () => {

    return (
        <Form className="container">
            <FormGroup>
                <FormLabel column={1}>

                </FormLabel>

            </FormGroup>

        </Form>
    )

}

export default GetProposes;